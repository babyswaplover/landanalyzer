<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.2.0/dist/js/bootstrap.bundle.min.js"></script>
    <title>Baby Wonderland Analyzer</title>
  </head>
  <body>
    <div class="container">
      <h1>Baby Wonderland Analyzer</h1>
      <h5 style="text-align:right">Fetched: <%=date %></h5>
      <div class="row">
        <table class="table table-striped">
          <thead>
            <tr>
              <th>Size</th>
              <th>Type</th>
              <th>Count</th>
            </tr>
          </thead>
          <tbody>
          <% for (const count of counts) { %>
            <tr>
              <td><%= `${count.regionWeight} × ${count.regionWeight}` %></td>
              <th><%= (count.level == 2) ? "Premium" : "Normal" %></th>
              <th><%= count.count %></th>
            </tr>
          <% } %>
          </tbody>
        </table>
      </div>

      <div class="row">
        <form onsubmit="return setAddress();">
          <label for="address">Wallet Address:</label>
          <input type="text" id="address" name="address" class="form-control" placeholder="Address (0x...)" pattern="0x[0-9A-Fa-f]{40}" required>
          <button type="submit" class="btn btn-primary" style="margin-top:30px">Submit</button>
          <script>
            function setAddress() {
              const address = document.getElementById('address').value;
              window.location.href=`/address/${address}`;
              return false;
            }
          </script>
        </form>
      </div>
  </div>
  </body>
</html>
